import 'package:flutter/material.dart';

class addgrouppersonpage extends StatelessWidget {
  const addgrouppersonpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
