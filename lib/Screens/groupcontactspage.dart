import 'package:flutter/material.dart';

class groupcontactspage extends StatelessWidget {
  const groupcontactspage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
